from setuptools import setup

setup (name = 'pyintfft',
       version = '0.1.0',
       description = 'Integer Fourier Transform in Python',
       install_requires = ['numpy'],
       packages=['intfft'])

